extends layout

block content
  h1= "My todos"
  p Welcome to #{title}
  input(type="text", id = "input-name")
  input(type="text", id ="input-task")
  button.btn(type="submit", id  = "submit-data") Submit
  button.btn(type="submit", id =  "search") Search
  input(type="text", id ="search-name")
  
